requests>=2.31
beautifulsoup4>=4.12   # HTML parsing for image discovery + page facts
reportlab>=4.0         # PDF guide for Fudge
anthropic>=1.0         # Claude rewrites product_summary and writes the guide (ANTHROPIC_API_KEY)
higgsfield-client>=0.1 # Higgsfield Cloud image generation (HF_KEY)
playwright>=1.44       # optional: headless Chromium for JS-rendered competitor pages (`python -m playwright install chromium`)
# optional template readers: python-docx (.docx templates), pypdf (.pdf templates)
